main() {
  // declare the variable, i.e. int, float, double, char, etc.
  // initialize the variable

  /* use the variable:
  printf("zero padding in back of a number: [fill this part in]);
  printf("zero padding in front of a number: [fill this part in]);
  printf("exponentials (base 10): [fill this part in]);
  printf("truncation vs rounding of numbers: [fill this part in]);
  printf("zero padding in back of a number: [fill this part in]);
  printf("negative numbers: [fill this part in]);
  */
}